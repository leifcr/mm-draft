source 'http://rubygems.org'

gem 'rails', '~>3.1.2'
gem 'mongo'
gem 'mongo_ext'
gem 'bson_ext'
gem 'mongo_mapper'

group :test do
  gem "shoulda", ">= 2.11.3"
  # RAMDIV - acts as tree is broken with latest MM/Rails, support removed. will move to other solution later
  # gem 'ramdiv-mongo_mapper_acts_as_tree', ">=0.1.2" #:git => 'git://github.com/ramdiv/mongo_mapper_acts_as_tree.git' #, :require => 'mongo_mapper_acts_as_tree'
end
